server {
    listen 80;
    
    # 転送ルール
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # 転送ルール(API)
    location /api/ {
        proxy_pass http://flask:8080/;
    }

}
